import React from 'react';
import { Typography } from '@mui/material';

const ProductPage = () => {
  return <Typography variant="h4">Gestion des Produits</Typography>;
};

export default ProductPage;